//Initialize an array of numbers.
const list1:number[]=[1,2,3,4,5];

// Using map to create a new list where each number is twice its original value
const list = list1.map(number => number * 2);
//Output the result with double numbers list.
console.log(list)

