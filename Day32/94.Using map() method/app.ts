//Define an array of strings
const items: string[] = ["Apple", "Orange", "Pineapple", "Banana"];
//find the lenght of each string and store in a new array
const itemsLength = items.map((word) => word.length);
//Call the new array 'itemsLength'.
console.log(itemsLength);
