we created a function 'stringToInteger' with an argument 'strToInt' this function is designed to convert the string data type to a number data type. inside the function, we defined a method parseInt() which is used to convert string data type to number data type the return keyword returns the final value. At the end, we pass a string argument through the function to return its data type to a number.