//Define a function that convert string to an integer.
function stringToInteger(strToInt) {
    //this method used to convert the string type to number.
    return parseInt(strToInt);
}
//Call the function with an argument "2024". log the result.
console.log(stringToInteger("2024"));
