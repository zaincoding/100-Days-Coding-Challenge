Create a new date object 'currentDate' representing the current date and time.then extract day, month, year from the
current date using 'getDate()','getMonth()','getFullYear()' methods, extracting month we use +1 as month start from zero.
we defined a variable 'dayFormat' with condition if day is less than '10' add zero and similarly with 'monthFormat'.
finally concatanate 'dayFormat',monthFormat','year' and store the value in a variable 'dateFormat' Output it to the
console.to get the result in a defined format. 