The function 'decimals' defined with two parameters it's type number. inside the the function the product of the two numbers is calculated and
assined it's result to 'addNumber' variable. the 'Math.round()' function is used to round the product to two decimal places, it's result is stored in roundNumber
variable.The function 'decimals' is called with two numbers '5.6,4.23' as argument. 