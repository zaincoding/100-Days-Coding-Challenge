//Define a function that takes two parameters of type number.
function Addition(firstNumber: number, secondNumber: number) {
  //add and return the product of first and second number
  return firstNumber + secondNumber;
}
//Log the function to the console.
console.log(Addition(5, 5));
