In this code the function named greetUser is defined taking two parameter with default values and it's type srting
inside the function block the parameters concatenated in a string. the function once called with one argument and it display
the input value and the default value. once it called without passing any argument so it's displaying both the default values