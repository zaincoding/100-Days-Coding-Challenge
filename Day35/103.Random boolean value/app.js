//Define a variable 'random' to get random value.
var random = Math.random();
//if random number is grater than 0.5 display tru,otherwise false
var boolean = random > 0.5;
//Output the result.
console.log(boolean);
