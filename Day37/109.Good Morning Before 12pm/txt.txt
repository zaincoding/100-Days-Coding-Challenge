In this, we declare a variable called 'currentDate' to store the instance of the date object (date & time) in it. moreover using the 'getHours()' and 'getMinutes()' methods of the date object to extract hours and time from the current date then creating a string that represents hours and minutes. finally, use the if condition to check if it is time before noon if it is then run the if condition if it is not then run the else condition.