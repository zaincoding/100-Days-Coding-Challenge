We create two array of laptoprice and laptoprice2 which contining these two laptop prices.
and then use ... spread operator to combine the two arrays at the end we used the sort method with
calback function to sort out the combined array in an assending order.
