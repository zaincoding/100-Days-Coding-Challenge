// Define the function city_country with two parameters: city and country
function city_country(city:string, country:string){
//    // Log the combined string of city and country in the specified format
  console.log(`"${city}, ${country}"`)
}
// Call the function city_country with city and country name.
city_country('Karachi','Pakistan')

city_country('London','United Kingdom')

city_country('New York','USA')